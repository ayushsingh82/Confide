# The frontend no longer calls NEAR AI Cloud directly — /api/chat proxies to
# the Confide backend (see backend/.env.example), which holds NEAR_API_KEY
# and persists usage events to data/usage.jsonl.
NEXT_PUBLIC_BACKEND_URL=http://localhost:4000
